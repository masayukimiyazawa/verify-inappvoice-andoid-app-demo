<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Audio route for phone calls -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:name=".VoiceVerifyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoiceVerify"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <activity
            android:name=".ui.login.PhoneInputActivity"
            android:exported="true"
            android:theme="@style/Theme.VoiceVerify"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.VerifyCodeActivity"
            android:exported="false"
            android:theme="@style/Theme.VoiceVerify"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.dialpad.DialPadActivity"
            android:exported="false"
            android:theme="@style/Theme.VoiceVerify"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.call.CallActivity"
            android:exported="false"
            android:theme="@style/Theme.VoiceVerify.Call"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.call.IncomingCallActivity"
            android:exported="false"
            android:theme="@style/Theme.VoiceVerify.Call"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="portrait" />

        <!-- Vonage InApp Voice Service -->
        <service
            android:name=".service.VonageVoiceService"
            android:exported="false"
            android:foregroundServiceType="phoneCall" />

        <!-- Firebase Messaging Service -->
        <service
            android:name=".service.FirebaseMessagingService"
            android:exported="false" />

        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="true" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />

    </application>

</manifest>
